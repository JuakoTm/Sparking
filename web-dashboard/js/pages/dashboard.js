// js/pages/dashboard.js
// Loader module for the dashboard page.
// Keeps `js/main.js` intact and executes it when the page loads.
import '../main.js';

// Export nothing; this module exists to group page-specific entrypoints under `js/pages/`
